meta {
  name: Confirm email
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/confirm-email
  body: formUrlEncoded
  auth: bearer
}

body:form-urlencoded {
  confirmationToken: {{confirmationToken}}
}
